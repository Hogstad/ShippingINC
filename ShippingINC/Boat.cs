namespace ShippingINC
{
    class Boat
    {
        public int Size;
        public int TimeInPort;

        public Boat(int size, int timeInPort)
        {
            Size = size;
            TimeInPort = timeInPort;
        }
    }
}
